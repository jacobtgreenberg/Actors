<!DOCTYPE html>
<html lang="en">
<head>
   <%- include ('./partials/head.ejs') %>
</head>
<body>
    <%- include ('./partials/nav.ejs') %>
    <main class="indexMain">
        <% for(let i = 0;i < actors.length; i++) { %>
        <div class="indexActor">
            <a href="<%=actors[i].id%>">
             <img src= "<%= actors[i].img %>" >
            </a> <br>
            <a href="<%=actors[i].id%>"> <%= actors[i].name %></a>
        </div>
        <% } %>
    </main>
</body>
</html>