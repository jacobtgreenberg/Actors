<!DOCTYPE html>
<html lang="en">
<head>
   <%- include ('./partials/head.ejs') %>
</head>
<body>
    <%- include ('./partials/nav.ejs') %>
    <main class="actorMain">
            <div class="left">
                <img id="actorImg" src= "<%= actor.img %>" >
                <p> <%= actor.name %></p>
            </div>
            <div class="right">
                <p> <%= actor.bio %> </p>
                <a href= "/<%=actor.id%>/edit">Edit</a>
                <form action="/<%=actor.id%>?_method=DELETE" method="POST">
                    <input type=Submit value="Delete Entry">
                </form>
            </div>
    </main>
</body>
</html>